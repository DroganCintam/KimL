{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/kiml",
            "args": ["${workspaceRoot}/examples/event.kiml"],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}